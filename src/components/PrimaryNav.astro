---
const { items = [] } = Astro.props;
// each item: { href, label }
---

<nav class="mt-8 flex flex-col gap-3 text-lg">
  {items.map((i) => (
    <a
      href={i.href}
      class="text-neutral-400 hover:text-neutral-100 transition-colors"
    >
      {i.label}
    </a>
  ))}
</nav>
